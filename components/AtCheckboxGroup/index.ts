import AtCheckboxGroup from './AtCheckboxGroup.vue';

/* istanbul ignore next */
AtCheckboxGroup.install = (Vue: any) => {
  Vue.component(AtCheckboxGroup.name, AtCheckboxGroup);
};

export default AtCheckboxGroup;
