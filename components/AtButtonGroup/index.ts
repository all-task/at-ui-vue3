import AtButtonGroup from './AtButtonGroup.vue';

/* istanbul ignore next */
AtButtonGroup.install = (Vue: any) => {
  Vue.component(AtButtonGroup.name, AtButtonGroup);
};

export default AtButtonGroup;
